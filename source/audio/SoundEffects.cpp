#include "SoundEffects.h"

/*
SoundEffects::SoundEffects()
{
	sf::SoundBuffer sb;
	// button 1
	sb.loadFromFile("data/audio/se/button1.wav");
	soundBuffers.push_back(sb);
	button1.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// button 2
	sb.loadFromFile("data/audio/se/button2.wav");
	soundBuffers.push_back(sb);
	button2.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// building created
	sb.loadFromFile("data/audio/se/building-created.wav");
	soundBuffers.push_back(sb);
	buildingCreated.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// building destroyed
	sb.loadFromFile("data/audio/se/building-destroyed.wav");
	soundBuffers.push_back(sb);
	buildingDestroyed.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// cpu touched
	sb.loadFromFile("data/audio/se/cpu-touched.wav");
	soundBuffers.push_back(sb);
	cpuTouched.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// multi shoot bullet
	sb.loadFromFile("data/audio/se/multi-shoot.wav");
	soundBuffers.push_back(sb);
	multiBullet.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// defender shoot bullet
	sb.loadFromFile("data/audio/se/defender-shoot.wav");
	soundBuffers.push_back(sb);
	defenderBullet.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// wave begin
	sb.loadFromFile("data/audio/se/wave.wav");
	soundBuffers.push_back(sb);
	waveBegin.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// explosion
	sb.loadFromFile("data/audio/se/explosion.wav");
	soundBuffers.push_back(sb);
	explosion.setBuffer(soundBuffers[soundBuffers.size() - 1]);

	// enemy destroyed
	sb.loadFromFile("data/audio/se/enemy-destroyed.wav");
	soundBuffers.push_back(sb);
	enemyDestroyed.setBuffer(soundBuffers[soundBuffers.size() - 1]);
}

sf::Sound SoundEffects::button1;
sf::Sound SoundEffects::button2;
sf::Sound SoundEffects::buildingCreated;
sf::Sound SoundEffects::buildingDestroyed;
sf::Sound SoundEffects::cpuTouched;
sf::Sound SoundEffects::multiBullet;
sf::Sound SoundEffects::defenderBullet;
sf::Sound SoundEffects::waveBegin;
sf::Sound SoundEffects::explosion;
sf::Sound SoundEffects::enemyDestroyed;
std::vector<sf::SoundBuffer> SoundEffects::soundBuffers;
*/